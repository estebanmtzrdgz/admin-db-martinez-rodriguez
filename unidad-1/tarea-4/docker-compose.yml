services:
  mysql_db:
    build: .
    image: inventario-mysql:8.0
    container_name: mysql_inventario
    environment:
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3307:3306"
    volumes:
      - db_data:/var/lib/mysql
 
volumes:
  db_data:
 